<view class='content'>
  <image mode="{{item.mode}}" src="{{images}}" class='big'></image>
  <view class='small' wx:if="{{types==200}}">
  <image style="background-color: #eeeeee;" mode="{{item.mode}}" src="{{images}}" class='big'></image>
  </view>
  <view class='title'>
    <view class='title-top'>{{typea}}</view>
    <view>{{typeb}}</view>
    <view class='title-bottom'>。</view>
  </view>
  <view class='details'>{{ content }}。</view>
</view>

