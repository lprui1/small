<view class="prelike">  
  <f-prehead avatarUrls="{{avatarUrl}}" nickName="{{nickName}}"/>
  <button wx:if="{{canIUse}}" open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo" wx:if="{{!nickName}}">授权登录</button>
  <f-prelist Prelist="{{prelist}}" count="{{likenum.count}}"/>
</view>