<view class="prelike">  
  <f-prehead avatarUrls="{{avatarUrl}}" nickName="{{nickName}}" likenum="{{likenum}}"/>
  <button wx:if="{{canIUse}}" open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo" wx:if="{{!nickName}}" class='login'></button>
  <f-prelist Prelist="{{prelist}}"/>
</view>